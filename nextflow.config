manifest {
    name = 'nf-dynamic-outdir-test'
    description = 'Minimal pipeline to test NXF_FILE_ROOT for dynamic per-run output directories'
    mainScript = 'main.nf'
    nextflowVersion = '>=24.04.0'
}
